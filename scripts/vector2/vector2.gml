/// @description  array vector2(x, y);
/// @param x
/// @param  y
// there's really no reason to use this over array_compose but i think
// it looks nice so i'm keeping it

return array_compose(argument0, argument1);
