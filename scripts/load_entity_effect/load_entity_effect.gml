/// @description  bool load_entity_effect(buffer, Entity, version);
/// @param buffer
/// @param  Entity
/// @param  version

var static=load_entity(argument0, argument1, argument2);

// you most likely want this to be false, but
return static;
