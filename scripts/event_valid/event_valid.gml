/// @description  boolean event_valid(Entity, DataInstantiatedEvent);
/// @param Entity
/// @param  DataInstantiatedEvent

var entity=argument0;
var ievent=argument1;

// later on you want to evaluate the conditions but that can wait

return true;
