event_inherited();

name="Base Egg Group";

restricted=false;       // false if this group can breed with itself, true if it can't (undiscovered)
unrestricted=false;     // true if this group can breed with anything (ditto), false if it can't

// if you want to you could set up a matrix like the ones in DataType
// that make an egg group compatible with multiple other egg groups.
// there isn't a precedent in Pokémon for that though, so I'm not
// going to go that far.

